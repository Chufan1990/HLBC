syntax = "proto3";

package autoagric.control.calibrationtable;

message ControlCalibrationTable {
  repeated ControlCalibrationInfo calibration = 1;
}

message ControlCalibrationInfo {
  double speed = 1;
  double acceleration = 2;
  double command = 3;
}
